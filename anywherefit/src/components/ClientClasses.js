import React from 'react';
import ClassCard from './ClassCard';

const ClientClasses = ({activeUser, classList, withdraw}) => {
    return(
        <div>
            <h2>Classes I am enrolled in:</h2>
        </div>
    )
}

export default ClientClasses;